#!/usr/bin/ruby
$: << File.join(File.dirname(__FILE__), "..", "lib")

require 'provision/build'
require 'yaml'

build = Provision::Build.new()
build.load('../lib/config/ubuntuprecise.rb')
build.load('../lib/config/puppetmaster.rb')
build.provision("puppetmaster")